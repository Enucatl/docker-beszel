# Compose loads the shared homelab env for DOCKER_DOMAIN (see /opt/docker/.env).
COMPOSE_ENV_FILES=../.env

# Local agent registration (from hub "Add system" / universal token). Leave empty
# until the hub admin user exists; agent will restart until set.
BESZEL_AGENT_TOKEN=
BESZEL_AGENT_KEY=
